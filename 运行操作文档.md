# MY-LIVO2.0 编译与运行操作文档

本文适用于项目目录 `/home/project/MY-LIVO2.0`、ROS 2 Humble，以及由当前
GroundExtractor 生成的芜湖 29 号矿卡 ROS2 MCAP。

完整 LIVO 还要求相机消息和解码插件（它会同时安装
`ffmpeg_image_transport_msgs`）：

```bash
sudo apt update
sudo apt install ros-humble-ffmpeg-image-transport
```

只运行 LIO 时不需要该相机插件。可用
`ros2 pkg prefix ffmpeg_image_transport_msgs` 检查是否已安装。

## 1. 当前输入契约

- LiDAR：`/front_lidar`，`sensor_msgs/msg/PointCloud2`。
- LiDAR `header.stamp`：扫描起始/首点时间。
- LiDAR 逐点 `timestamp`：相对 `header.stamp` 的 `float32` 秒。
- IMU：`/imu_data`，RFU 坐标系（x 右、y 前、z 上），角速度单位
  `rad/s`，加速度单位 `m/s²`，使用原始比力。
- 组合导航：`/imu_data/odometry`，位置严格为
  `[st_point_3d.x, st_point_3d.y, f_pos_alt]`；输入参考点是车辆后轴，程序
  按标定杆臂换算到 IMU 中心。
- RTK 状态：`/imu_data/ins_status`。只有 keyframe 时刻对应的
  `ins_pos_mode == 4` 才可能进入 GTSAM，状态失效、时间无 bracket 或创新过大
  都会记录明确拒绝原因。
- 相机：`/midrange_camera/ffmpeg`，只在 LIVO 模式启用。

LiDAR 时间规则已经写入程序内部，不存在
`lidar_header_is_scan_end` 参数。程序直接使用消息头作为扫描起始时间，不再
人为减 100 ms。

对三个 Perception MCAP 的全部 900 帧实测结果为：每帧出现 19–34 个、全集
共 34 个活跃 `ring` ID，ID 稀疏分布且最大值为 126。程序的 `scan_line`
表示可接受的 `ring` ID 上界而非活跃线数，所以
`config/wuhu_truck29.yaml` 中必须配置为 128；配置成 34 会错误丢弃大量点。

launch 默认直接读取源码目录中的
`/home/project/MY-LIVO2.0/config/wuhu_truck29.yaml`。保存 YAML 后重启 launch
即可生效，不需要重新编译。需要临时使用另一份参数文件时传入
`config_file:=/绝对路径/file.yaml`。

## 2. 编译

不要在 Conda 环境中编译。构建脚本会清除 CMake 缓存后重新配置，因此目录
改名后不会继续引用旧路径。

Odometry Pose Graph 使用 `3rdparty/gtsam` 中的 GTSAM 4.2.2。主构建脚本发现
system-Eigen 版本尚未生成时会自动执行：

```bash
BUILD_JOBS=4 ./scripts/build_gtsam_system_eigen.sh
```

该隔离版本使用系统 Eigen 3.4.0 且关闭 TBB，以避免分别与 PCL 的 Eigen ABI、
OpenCV 的旧 TBB 依赖发生冲突。已有 `3rdparty/gtsam/build` 不会被覆盖。

```bash
cd /home/project/MY-LIVO2.0
bash scripts/build_ros2_humble.sh
```

如需限制并行度，例如使用 4 个任务：

```bash
BUILD_JOBS=4 bash scripts/build_ros2_humble.sh
```

编译成功后验证 ROS 包和 launch：

```bash
source /opt/ros/humble/setup.bash
source /home/project/MY-LIVO2.0/install/setup.bash
ros2 pkg prefix fast_livo
ros2 launch fast_livo wuhu_truck29.launch.py --show-args
```

`ros2 pkg prefix` 应返回 `/home/project/MY-LIVO2.0/install/fast_livo`。
`--show-args` 中不应出现 `lidar_header_is_scan_end`，并应显示源码目录下的
`config_file` 默认路径。

## 3. 首次准备运行包

MY-LIVO2.0 优先使用一个按时间归并并带索引的运行包。该操作不会修改原始
MCAP：

```bash
cd /home/project/MY-LIVO2.0
source /opt/ros/humble/setup.bash
source install/setup.bash
./scripts/prepare_wuhu_bag.py
```

默认输入：
`/home/project/data/haibo/huaining/03/ros2bag`

默认输出：
`/home/project/data/haibo/huaining/03/ros2bag_my_livo`

默认输出包含 LIVO 所需的相机话题。若只需要更小的 LIO 运行包，应使用另一
输出目录，避免与完整包混淆：

```bash
./scripts/prepare_wuhu_bag.py --lio-only \
  --output /home/project/data/haibo/huaining/03/ros2bag_my_livo_lio
```

准备脚本为保护已有结果不会覆盖非空输出目录。需要重做时请先人工确认并将
旧输出移到备份位置，再重新执行。

## 4. 运行 LiDAR-IMU 模式（LIO）

终端 1：

```bash
cd /home/project/MY-LIVO2.0
source /opt/ros/humble/setup.bash
source install/setup.bash
ros2 launch fast_livo wuhu_truck29.launch.py \
  use_camera:=false use_rviz:=true rear_axle_to_imu:=true
```

终端 2：

```bash
cd /home/project/MY-LIVO2.0
./scripts/play_all_bags.sh --lio-only --start-offset 10 --rate 1.0
```

## 5. 运行 LiDAR-IMU-Visual 模式（LIVO）

终端 1：

```bash
cd /home/project/MY-LIVO2.0
source /opt/ros/humble/setup.bash
source install/setup.bash
ros2 launch fast_livo wuhu_truck29.launch.py \
  use_camera:=true use_rviz:=true rear_axle_to_imu:=true
```

终端 2：

```bash
cd /home/project/MY-LIVO2.0
./scripts/play_all_bags.sh --with-camera --rate 1.0
```

从整段数据第 45 秒开始调试：

```bash
./scripts/play_all_bags.sh --lio-only --start-offset 10 --rate 1.0
```

新建地图应从包含静止初始化的开头播放；中途开始只适合排查和可视化。

## 6. 播放前检查

不发布数据，只检查包路径与播放计划：

```bash
./scripts/play_all_bags.sh --lio-only --dry-run
./scripts/play_all_bags.sh --with-camera --dry-run
```

运行中可另开终端检查：

```bash
source /opt/ros/humble/setup.bash
source /home/project/MY-LIVO2.0/install/setup.bash
ros2 topic hz /front_lidar
ros2 topic hz /imu_data
ros2 topic hz /imu_data/odometry
ros2 topic hz /imu_data/ins_status
ros2 topic echo /front_lidar --field header --once
```

主要输出应包含 `/cloud_registered`、`/aft_mapped_to_init`、`/path`、
`/imu_reference_odom`、`/imu_reference_path`、
`/backend/keyframe_path_raw`、`/backend/keyframe_path_optimized` 和
`/backend/odometry_optimized`、`/backend/verified_loops`、
`/backend/global_map_optimized`。TF 树还应包含 `mine -> odom`。

## 7. 后端阶段验收

回放结束以后执行：

```bash
cd /home/project/MY-LIVO2.0
./scripts/validate_backend_keyframes.py \
  --trajectory Log/result/wuhu_truck29.txt
./scripts/validate_loop_candidates.py
./scripts/validate_loop_registration.py
./scripts/validate_loop_verification.py
./scripts/validate_loop_pose_graph.py
./scripts/validate_rtk_fusion.py
./scripts/validate_optimized_global_map.py
```

必须在 mapping 节点正常退出、日志和最终 PCD 已落盘后执行。联合模式下 raw 与
optimized 轨迹通常不再重合，这是 Loop/RTK 约束生效的预期结果；
`validate_odometry_pose_graph.py` 只用于同时关闭 Loop 与 RTK 的消融测试。
最终地图保存于 `Log/backend/optimized_global_map.pcd`，完整优化轨迹保存于
`Log/backend/optimized_trajectory.csv`。

全局地图在线维护采用 60 m 瓦片、20 KF 周期追加、10 KF 图修正去抖和独立
工作线程；Loop 或较大修正会触发精确全建，节点退出时始终基于最终图位姿做一次
完整重建。完整 raw/optimized Path 每 10 KF 发布以控制长任务网络开销，但
`/backend/odometry_optimized` 与 `mine -> odom` 每个关键帧都会更新。

## 8. 常见问题

- 找不到 `fast_livo`：当前终端未执行 `source install/setup.bash`，或编译失败。
- 目录改名后 CMake 路径错误：重新运行 `bash scripts/build_ros2_humble.sh`；脚本
  已带 `--cmake-clean-cache`。
- 修改 `config/wuhu_truck29.yaml`：保存后重启 launch 即可，不要重新编译。
- 没有图像：确认 launch 使用 `use_camera:=true`，播放使用
  `--with-camera`，准备包不是通过 `--lio-only` 生成的，并已安装
  `ros-humble-ffmpeg-image-transport`。
- 初始轨迹明显漂移：从数据开头低速播放，保留静止 IMU 初始化段，并确认
  `/imu_data` 与 `/imu_data/odometry` 都在发布。
- 不要为当前 MCAP 添加或传入 `lidar_header_is_scan_end`；该接口已经删除。
